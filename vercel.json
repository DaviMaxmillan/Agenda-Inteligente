{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/agenda run build",
  "outputDirectory": "artifacts/agenda/dist/public",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://agenda-inteligente-production-3be4.up.railway.app/api/:path*"
    }
  ]
}